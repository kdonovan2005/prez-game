<h1>All Players</h1>

<% @players.each do |player| %>
  <a href="<%= player_path(player) %>"><img src="<%= player.pic_url %>"></a> <br>
  <%= link_to player.name, player_path(player) %> <br>
<% end %>

<a href="<%= new_player_path %>">Create a new player!</a>
