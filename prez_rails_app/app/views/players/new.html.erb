<h1>Create a new Player!</h1>
  <%= form_for(@player) do |f| %>

<label>Player Name:</label>
  <%= f.text_field :name %>

<label>Tagline</label>
  <%= f.text_area :tagline %>

<label>Picture URL</label>
  <%= f.text_field :pic_url %>

  <%= f.submit "You are not Michael Jackson" %>

<% end %>
